server:
  port: 8888
  servlet:
    context-path: /retail-store

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    hibernate:
      ddl-auto: update
  banner:
    location: banner-nyan-cat.txt

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/RetailStore
    username: postgres
    password: 0904622832
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


